import { Link } from "react-router-dom"

function NotFoundPage() {
      return (
        <> 
          <Link to="/">не знайдено, повернутися :(</Link>
        </>
    )
}

export default NotFoundPage